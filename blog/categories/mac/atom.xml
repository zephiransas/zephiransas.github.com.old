<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Mac | zephiransasのチラシの裏]]></title>
  <link href="http://zephiransas.github.io/blog/categories/mac/atom.xml" rel="self"/>
  <link href="http://zephiransas.github.io/"/>
  <updated>2015-12-23T23:49:17+09:00</updated>
  <id>http://zephiransas.github.io/</id>
  <author>
    <name><![CDATA[Takafumi Yoshida (@zephiransas)]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[TimeMachineで検証エラーが発生した場合の復旧方法]]></title>
    <link href="http://zephiransas.github.io/blog/2013/08/16/resque-timemachine/"/>
    <updated>2013-08-16T14:57:00+09:00</updated>
    <id>http://zephiransas.github.io/blog/2013/08/16/resque-timemachine</id>
    <content type="html"><![CDATA[<p>我が家の環境では、<a href="http://buffalo.jp/product/hdd/network/ls-xl/">BuffaloのLS-XLシリーズ</a>というNASを使って、MBAのTime Machine環境を作っているのですが、最近よくこんなメッセージが出るようになりました。</p>

<p><img src="/images/20130816/tm_error.png" alt="error" /></p>

<p>これはApple純正のTime Capsule以外をTime Machine環境に使用すると出てくるらしいです。
しかもこうなってしまうと、「後でバックアップを作成」にしても再度同じメッセージが表示されるし、「新規バックアップを作成」を選ぶと、それまでのバックアップの履歴は全て消えてしまいます。</p>

<p>なので、この場合の復旧手順をメモっておきます。</p>

<p>まずは、Time Machineを一旦「切」にしておきます。</p>

<p><img src="/images/20130816/tm_off.png" alt="off" /></p>

<p>次にNASをマウントして、そのマウントポイントまでのパスを確認しておきます。自分の場合は「/Volumes/share/unicorn.sparsebundle」でしたので、以降はこれで説明します。</p>

<p>次にターミナルを立ちあげ</p>

<p><code>bash
$ sudo chflags -R nouchg /Volumes/share/unicorn.sparsebundle
</code>
して、ロックを解除しておきます。次に</p>

<p><code>bash
$ hdiutil attach -nomount -noverify -noautofsck /Volumes/share/unicorn.sparsebundle
</code>
と入力します。実行すると以下のようなメッセージが表示されます。</p>

<p><code>bash
/dev/disk2 Apple_partition_scheme
/dev/disk2s1 Apple_partition_map
/dev/disk2s2 Apple_HFSX
</code>
この時の/dev/disk? の?部分は実行時の環境によって異なる数値が入っていますので注意。また、この次点でTime Machineイメージに対して修復処理がバックグラウンドで実行されています。そのログが/var/log/fsck_hfs.logに出力されています。復旧が終了したかどうかはそのログで確認するのでtailで眺めてやります。</p>

<p><code>bash
$ tail -f /var/log/fsck_hfs.log
</code></p>

<p>おそらくこんな感じ。</p>

<p><code>bash
** Checking Journaled HFS Plus volume.
** Detected a case-sensitive volume.
   The volume name is Time Machine バックアップ
** Checking extents overflow file.
** Checking catalog file.
** Checking multi-linked files.
** Checking catalog hierarchy.
** Checking extended attributes file.
** Checking multi-linked directories.
** Checking volume bitmap.
** Checking volume information.
** The volume Time Machine バックアップ appears to be OK.
</code></p>

<p>最後の「The volume Time Machine バックアップ appears to be OK.」が表示されればイメージの復旧は完了です。</p>

<p>終了したら、デタッチしておきます。</p>

<p><code>bash
hdiutil detach /dev/disk2s2
</code></p>

<p>ただこの状態だと、イメージ内部ではまだ壊れたままという情報が残っているため、これを修正します。</p>

<p><code>bash
$ cd /Volumes/share/unicorn.sparsebundle/
$ vi com.apple.TimeMachine.MachineID.plist
</code></p>

<p>とし、その内部の</p>

<p><code>xml
&lt;integer&gt;2&lt;/integer&gt;
</code></p>

<p>を</p>

<p><code>xml
&lt;integer&gt;0&lt;/integer&gt;
</code></p>

<p>に修正します。</p>

<p>これで全て完了です。Time Machineを「入」にして、バックアップを実行して、エラーが表示されなければ完了です。</p>

<p>え？根本的な解決になってない！ですって？　そりゃTime Machineを買うしかありませんがな！！ww</p>

<iframe src="http://rcm-fe.amazon-adsystem.com/e/cm?lt1=_blank&bc1=000000&IS2=1&bg1=FFFFFF&fc1=000000&lc1=0000FF&t=zephiransas-22&o=9&p=8&l=as4&m=amazon&f=ifr&ref=ss_til&asins=B00DCM3W26" style="width:120px;height:240px;" scrolling="no" marginwidth="0" marginheight="0" frameborder="0"></iframe>

]]></content>
  </entry>
  
</feed>
